[project]
name = "ase-convertor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colormath>=3.0.0",
    "pyinstaller>=6.14.2",
    "swatch>=0.4.0",
]
